version: '3.8'

services:
  movies.db:
    image: mysql:5.7
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=cricket_db
    volumes:
      - cricketdbdata:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: "mysql -uroot -ppassword cricket_db -e 'select 1'"
      timeout: 20s
      interval: 10s
      retries: 10

volumes:
  cricketdbdata:
